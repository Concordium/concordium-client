module WalletTypes where

{- Originally we started off by developing the wallet frontend standalone, well
before the gRPC endpoints existed. This file is a transient, as we move those
frontend API data stubs to the backend behind real endpoints, and then
eventually remove them entirely when they get implemented against gRPC endpoints
-}
